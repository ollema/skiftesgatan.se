# Production database config goes in .env.prod (see .env.prod.example)
# DATABASE_URL points at the public dev/test Postgres host (dummy data only).
DATABASE_URL=""
# DATABASE_URL_ADMIN connects to the admin database (postgres) on the same host.
# Used by db:reset:dev for DROP/CREATE on the dev database.
DATABASE_URL_ADMIN=""

ORIGIN=""

# Better Auth
# For production use 32 characters and generated with high entropy
# https://www.better-auth.com/docs/installation
BETTER_AUTH_SECRET=""

# Resend (email delivery)
RESEND_API_KEY=""
EMAIL_FROM="Skiftesgatan <info@brf.skiftesgatan.se>"

# Contact page (visible to logged-in users only)
CONTACT_MANAGER_NAME=""
CONTACT_MANAGER_PHONE=""
CONTACT_MANAGER_EMAIL=""
