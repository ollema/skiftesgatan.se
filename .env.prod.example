# SSH tunnel via CapRover SSH container (inside Docker overlay network)
# Used by: pnpm db:tunnel
PROD_SSH_HOST=
PROD_SSH_PORT=

# PostgreSQL connection (via tunnel on localhost:5432)
# Used by: pnpm db:push prod, pnpm db:studio prod
DATABASE_URL=
