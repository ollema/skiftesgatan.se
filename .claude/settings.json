{
	"hooks": {
		"Stop": [
			{
				"matcher": "",
				"hooks": [
					{
						"type": "command",
						"command": "git diff --quiet && git diff --cached --quiet || (pnpm format && pnpm lint && pnpm check && pnpm test 2>&1 | tail -20)",
						"timeout": 120
					}
				]
			}
		]
	}
}
